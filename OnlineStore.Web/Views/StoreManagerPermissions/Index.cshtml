@model IEnumerable<OnlineStore.Core.Entities.StoreEntity.StoreManagerPermissions>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Permission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PermissionStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StoreManagerId)
            </th>
         
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Permission)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StoreManagerId)
            </td>
          
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete",   new { id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
